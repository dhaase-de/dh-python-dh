language: python

python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "nightly"

install:
    - "./scripts/build-install.sh"
    - "pip install --requirement requirements.txt"

script:
    - "nosetests dh --with-doctest --verbosity=3"

